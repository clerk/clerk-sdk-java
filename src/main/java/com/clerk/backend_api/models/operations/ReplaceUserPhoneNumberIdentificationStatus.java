/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.clerk.backend_api.models.operations;

import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.String;
import java.util.Objects;
import java.util.Optional;

/**
 * ReplaceUserPhoneNumberIdentificationStatus
 * 
 * <p>Controls the status of the replacement phone number. Defaults to `verified`. Set to
 * `reserved` to create it reserved (unverified but usable for sign-in and locked so no
 * other user can claim it), or to `unverified` to create it neither usable for sign-in
 * nor locked.
 * 
 * <p>**Warning:** `unverified` can lock the user out of their account. An unverified phone
 * number cannot be used to sign in, so if the user has no other verified or reserved
 * identifier, they will be unable to authenticate and unable to verify this number.
 * Prefer `reserved` unless you specifically need the number left unclaimed — for
 * example so that another user can also hold it until one of them verifies it.
 */
public enum ReplaceUserPhoneNumberIdentificationStatus {
    VERIFIED("verified"),
    RESERVED("reserved"),
    UNVERIFIED("unverified");

    @JsonValue
    private final String value;

    ReplaceUserPhoneNumberIdentificationStatus(String value) {
        this.value = value;
    }
    
    public String value() {
        return value;
    }
    
    public static Optional<ReplaceUserPhoneNumberIdentificationStatus> fromValue(String value) {
        for (ReplaceUserPhoneNumberIdentificationStatus o: ReplaceUserPhoneNumberIdentificationStatus.values()) {
            if (Objects.deepEquals(o.value, value)) {
                return Optional.of(o);
            }
        }
        return Optional.empty();
    }
}

