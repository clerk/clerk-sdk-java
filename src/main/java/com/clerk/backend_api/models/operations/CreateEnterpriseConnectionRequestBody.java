/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.clerk.backend_api.models.operations;

import com.clerk.backend_api.utils.Utils;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import java.lang.Boolean;
import java.lang.Override;
import java.lang.String;
import java.lang.SuppressWarnings;
import java.util.List;
import org.openapitools.jackson.nullable.JsonNullable;


public class CreateEnterpriseConnectionRequestBody {
    /**
     * The display name of the connection
     */
    @JsonProperty("name")
    private String name;

    /**
     * The identity provider (e.g. saml_custom, oidc_custom)
     */
    @JsonProperty("provider")
    private Provider provider;

    /**
     * Domains associated with the enterprise connection (required; at least one). Values are normalized to
     * lowercase. Each domain must be a valid fully qualified domain name.
     */
    @JsonProperty("domains")
    private List<String> domains;

    /**
     * Organization ID when the connection is linked to an organization
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("organization_id")
    private JsonNullable<String> organizationId;

    /**
     * Whether the enterprise connection is active. When true, IdP metadata must be provided via the `saml`
     * object.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("active")
    private JsonNullable<Boolean> active;

    /**
     * SAML connection-specific properties. Only applied when the enterprise connection uses SAML (e.g.
     * provider is saml_custom).
     * Use this to set IdP configuration, attribute mapping, and other SAML-specific settings at creation
     * time.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("saml")
    private JsonNullable<? extends Saml> saml;

    /**
     * OIDC connection-specific properties. Only applied when the enterprise connection uses OIDC (e.g.
     * provider is oidc_custom).
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("oidc")
    private JsonNullable<? extends Oidc> oidc;

    @JsonCreator
    public CreateEnterpriseConnectionRequestBody(
            @JsonProperty("name") String name,
            @JsonProperty("provider") Provider provider,
            @JsonProperty("domains") List<String> domains,
            @JsonProperty("organization_id") JsonNullable<String> organizationId,
            @JsonProperty("active") JsonNullable<Boolean> active,
            @JsonProperty("saml") JsonNullable<? extends Saml> saml,
            @JsonProperty("oidc") JsonNullable<? extends Oidc> oidc) {
        Utils.checkNotNull(name, "name");
        Utils.checkNotNull(provider, "provider");
        Utils.checkNotNull(domains, "domains");
        Utils.checkNotNull(organizationId, "organizationId");
        Utils.checkNotNull(active, "active");
        Utils.checkNotNull(saml, "saml");
        Utils.checkNotNull(oidc, "oidc");
        this.name = name;
        this.provider = provider;
        this.domains = domains;
        this.organizationId = organizationId;
        this.active = active;
        this.saml = saml;
        this.oidc = oidc;
    }
    
    public CreateEnterpriseConnectionRequestBody(
            String name,
            Provider provider,
            List<String> domains) {
        this(name, provider, domains,
            JsonNullable.undefined(), JsonNullable.undefined(), JsonNullable.undefined(),
            JsonNullable.undefined());
    }

    /**
     * The display name of the connection
     */
    @JsonIgnore
    public String name() {
        return name;
    }

    /**
     * The identity provider (e.g. saml_custom, oidc_custom)
     */
    @JsonIgnore
    public Provider provider() {
        return provider;
    }

    /**
     * Domains associated with the enterprise connection (required; at least one). Values are normalized to
     * lowercase. Each domain must be a valid fully qualified domain name.
     */
    @JsonIgnore
    public List<String> domains() {
        return domains;
    }

    /**
     * Organization ID when the connection is linked to an organization
     */
    @JsonIgnore
    public JsonNullable<String> organizationId() {
        return organizationId;
    }

    /**
     * Whether the enterprise connection is active. When true, IdP metadata must be provided via the `saml`
     * object.
     */
    @JsonIgnore
    public JsonNullable<Boolean> active() {
        return active;
    }

    /**
     * SAML connection-specific properties. Only applied when the enterprise connection uses SAML (e.g.
     * provider is saml_custom).
     * Use this to set IdP configuration, attribute mapping, and other SAML-specific settings at creation
     * time.
     */
    @SuppressWarnings("unchecked")
    @JsonIgnore
    public JsonNullable<Saml> saml() {
        return (JsonNullable<Saml>) saml;
    }

    /**
     * OIDC connection-specific properties. Only applied when the enterprise connection uses OIDC (e.g.
     * provider is oidc_custom).
     */
    @SuppressWarnings("unchecked")
    @JsonIgnore
    public JsonNullable<Oidc> oidc() {
        return (JsonNullable<Oidc>) oidc;
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * The display name of the connection
     */
    public CreateEnterpriseConnectionRequestBody withName(String name) {
        Utils.checkNotNull(name, "name");
        this.name = name;
        return this;
    }

    /**
     * The identity provider (e.g. saml_custom, oidc_custom)
     */
    public CreateEnterpriseConnectionRequestBody withProvider(Provider provider) {
        Utils.checkNotNull(provider, "provider");
        this.provider = provider;
        return this;
    }

    /**
     * Domains associated with the enterprise connection (required; at least one). Values are normalized to
     * lowercase. Each domain must be a valid fully qualified domain name.
     */
    public CreateEnterpriseConnectionRequestBody withDomains(List<String> domains) {
        Utils.checkNotNull(domains, "domains");
        this.domains = domains;
        return this;
    }

    /**
     * Organization ID when the connection is linked to an organization
     */
    public CreateEnterpriseConnectionRequestBody withOrganizationId(String organizationId) {
        Utils.checkNotNull(organizationId, "organizationId");
        this.organizationId = JsonNullable.of(organizationId);
        return this;
    }

    /**
     * Organization ID when the connection is linked to an organization
     */
    public CreateEnterpriseConnectionRequestBody withOrganizationId(JsonNullable<String> organizationId) {
        Utils.checkNotNull(organizationId, "organizationId");
        this.organizationId = organizationId;
        return this;
    }

    /**
     * Whether the enterprise connection is active. When true, IdP metadata must be provided via the `saml`
     * object.
     */
    public CreateEnterpriseConnectionRequestBody withActive(boolean active) {
        Utils.checkNotNull(active, "active");
        this.active = JsonNullable.of(active);
        return this;
    }

    /**
     * Whether the enterprise connection is active. When true, IdP metadata must be provided via the `saml`
     * object.
     */
    public CreateEnterpriseConnectionRequestBody withActive(JsonNullable<Boolean> active) {
        Utils.checkNotNull(active, "active");
        this.active = active;
        return this;
    }

    /**
     * SAML connection-specific properties. Only applied when the enterprise connection uses SAML (e.g.
     * provider is saml_custom).
     * Use this to set IdP configuration, attribute mapping, and other SAML-specific settings at creation
     * time.
     */
    public CreateEnterpriseConnectionRequestBody withSaml(Saml saml) {
        Utils.checkNotNull(saml, "saml");
        this.saml = JsonNullable.of(saml);
        return this;
    }

    /**
     * SAML connection-specific properties. Only applied when the enterprise connection uses SAML (e.g.
     * provider is saml_custom).
     * Use this to set IdP configuration, attribute mapping, and other SAML-specific settings at creation
     * time.
     */
    public CreateEnterpriseConnectionRequestBody withSaml(JsonNullable<? extends Saml> saml) {
        Utils.checkNotNull(saml, "saml");
        this.saml = saml;
        return this;
    }

    /**
     * OIDC connection-specific properties. Only applied when the enterprise connection uses OIDC (e.g.
     * provider is oidc_custom).
     */
    public CreateEnterpriseConnectionRequestBody withOidc(Oidc oidc) {
        Utils.checkNotNull(oidc, "oidc");
        this.oidc = JsonNullable.of(oidc);
        return this;
    }

    /**
     * OIDC connection-specific properties. Only applied when the enterprise connection uses OIDC (e.g.
     * provider is oidc_custom).
     */
    public CreateEnterpriseConnectionRequestBody withOidc(JsonNullable<? extends Oidc> oidc) {
        Utils.checkNotNull(oidc, "oidc");
        this.oidc = oidc;
        return this;
    }

    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        CreateEnterpriseConnectionRequestBody other = (CreateEnterpriseConnectionRequestBody) o;
        return 
            Utils.enhancedDeepEquals(this.name, other.name) &&
            Utils.enhancedDeepEquals(this.provider, other.provider) &&
            Utils.enhancedDeepEquals(this.domains, other.domains) &&
            Utils.enhancedDeepEquals(this.organizationId, other.organizationId) &&
            Utils.enhancedDeepEquals(this.active, other.active) &&
            Utils.enhancedDeepEquals(this.saml, other.saml) &&
            Utils.enhancedDeepEquals(this.oidc, other.oidc);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            name, provider, domains,
            organizationId, active, saml,
            oidc);
    }
    
    @Override
    public String toString() {
        return Utils.toString(CreateEnterpriseConnectionRequestBody.class,
                "name", name,
                "provider", provider,
                "domains", domains,
                "organizationId", organizationId,
                "active", active,
                "saml", saml,
                "oidc", oidc);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private String name;

        private Provider provider;

        private List<String> domains;

        private JsonNullable<String> organizationId = JsonNullable.undefined();

        private JsonNullable<Boolean> active = JsonNullable.undefined();

        private JsonNullable<? extends Saml> saml = JsonNullable.undefined();

        private JsonNullable<? extends Oidc> oidc = JsonNullable.undefined();

        private Builder() {
          // force use of static builder() method
        }


        /**
         * The display name of the connection
         */
        public Builder name(String name) {
            Utils.checkNotNull(name, "name");
            this.name = name;
            return this;
        }


        /**
         * The identity provider (e.g. saml_custom, oidc_custom)
         */
        public Builder provider(Provider provider) {
            Utils.checkNotNull(provider, "provider");
            this.provider = provider;
            return this;
        }


        /**
         * Domains associated with the enterprise connection (required; at least one). Values are normalized to
         * lowercase. Each domain must be a valid fully qualified domain name.
         */
        public Builder domains(List<String> domains) {
            Utils.checkNotNull(domains, "domains");
            this.domains = domains;
            return this;
        }


        /**
         * Organization ID when the connection is linked to an organization
         */
        public Builder organizationId(String organizationId) {
            Utils.checkNotNull(organizationId, "organizationId");
            this.organizationId = JsonNullable.of(organizationId);
            return this;
        }

        /**
         * Organization ID when the connection is linked to an organization
         */
        public Builder organizationId(JsonNullable<String> organizationId) {
            Utils.checkNotNull(organizationId, "organizationId");
            this.organizationId = organizationId;
            return this;
        }


        /**
         * Whether the enterprise connection is active. When true, IdP metadata must be provided via the `saml`
         * object.
         */
        public Builder active(boolean active) {
            Utils.checkNotNull(active, "active");
            this.active = JsonNullable.of(active);
            return this;
        }

        /**
         * Whether the enterprise connection is active. When true, IdP metadata must be provided via the `saml`
         * object.
         */
        public Builder active(JsonNullable<Boolean> active) {
            Utils.checkNotNull(active, "active");
            this.active = active;
            return this;
        }


        /**
         * SAML connection-specific properties. Only applied when the enterprise connection uses SAML (e.g.
         * provider is saml_custom).
         * Use this to set IdP configuration, attribute mapping, and other SAML-specific settings at creation
         * time.
         */
        public Builder saml(Saml saml) {
            Utils.checkNotNull(saml, "saml");
            this.saml = JsonNullable.of(saml);
            return this;
        }

        /**
         * SAML connection-specific properties. Only applied when the enterprise connection uses SAML (e.g.
         * provider is saml_custom).
         * Use this to set IdP configuration, attribute mapping, and other SAML-specific settings at creation
         * time.
         */
        public Builder saml(JsonNullable<? extends Saml> saml) {
            Utils.checkNotNull(saml, "saml");
            this.saml = saml;
            return this;
        }


        /**
         * OIDC connection-specific properties. Only applied when the enterprise connection uses OIDC (e.g.
         * provider is oidc_custom).
         */
        public Builder oidc(Oidc oidc) {
            Utils.checkNotNull(oidc, "oidc");
            this.oidc = JsonNullable.of(oidc);
            return this;
        }

        /**
         * OIDC connection-specific properties. Only applied when the enterprise connection uses OIDC (e.g.
         * provider is oidc_custom).
         */
        public Builder oidc(JsonNullable<? extends Oidc> oidc) {
            Utils.checkNotNull(oidc, "oidc");
            this.oidc = oidc;
            return this;
        }

        public CreateEnterpriseConnectionRequestBody build() {

            return new CreateEnterpriseConnectionRequestBody(
                name, provider, domains,
                organizationId, active, saml,
                oidc);
        }

    }
}
