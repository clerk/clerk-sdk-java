/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

package com.clerk.backend_api.models.components;

import com.clerk.backend_api.utils.GenericTypeIdResolver;
import java.lang.IllegalArgumentException;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;


public class VerificationTypeIdResolver extends GenericTypeIdResolver<Verification> {

    public VerificationTypeIdResolver() {
        super(UnknownVerification.class);
        initializeTypeMap();
    }

    private void initializeTypeMap() {
        registerType("verification_otp", Otp.class);
        registerType("verification_admin", Admin.class);
        registerType("verification_from_oauth", FromOAuth.class);
        registerType("verification_ticket", Ticket.class);
        registerType("verification_saml", Saml.class);
        registerType("verification_email_link", EmailLink.class);
    }

    @Override
    public String idFromValue(Object value) {
        if (value == null) {
            return null;
        }
        
        // Handle known types by checking if they implement the discriminator method
        if (value instanceof Verification) {
            Verification discriminated = (Verification) value;
            return discriminated.object();
        }
        
        throw new IllegalArgumentException("Unknown value type: " + value.getClass().getName());
    }

    @Override
    public String getDescForKnownTypeIds() {
        return "Verification type resolver";
    }

}