/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.clerk.backend_api.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class VerificationAdminVerificationStrategy {

    public static final VerificationAdminVerificationStrategy ADMIN = new VerificationAdminVerificationStrategy("admin");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, VerificationAdminVerificationStrategy> values = createValuesMap();
    private static final Map<String, VerificationAdminVerificationStrategyEnum> enums = createEnumsMap();

    private final String value;

    private VerificationAdminVerificationStrategy(String value) {
        this.value = value;
    }

    /**
     * Returns a VerificationAdminVerificationStrategy with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as VerificationAdminVerificationStrategy
     */ 
    @JsonCreator
    public static VerificationAdminVerificationStrategy of(String value) {
        synchronized (VerificationAdminVerificationStrategy.class) {
            return values.computeIfAbsent(value, v -> new VerificationAdminVerificationStrategy(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<VerificationAdminVerificationStrategyEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        VerificationAdminVerificationStrategy other = (VerificationAdminVerificationStrategy) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "VerificationAdminVerificationStrategy [value=" + value + "]";
    }

    // return an array just like an enum
    public static VerificationAdminVerificationStrategy[] values() {
        synchronized (VerificationAdminVerificationStrategy.class) {
            return values.values().toArray(new VerificationAdminVerificationStrategy[] {});
        }
    }

    private static final Map<String, VerificationAdminVerificationStrategy> createValuesMap() {
        Map<String, VerificationAdminVerificationStrategy> map = new LinkedHashMap<>();
        map.put("admin", ADMIN);
        return map;
    }

    private static final Map<String, VerificationAdminVerificationStrategyEnum> createEnumsMap() {
        Map<String, VerificationAdminVerificationStrategyEnum> map = new HashMap<>();
        map.put("admin", VerificationAdminVerificationStrategyEnum.ADMIN);
        return map;
    }
    
    
    public enum VerificationAdminVerificationStrategyEnum {

        ADMIN("admin"),;

        private final String value;

        private VerificationAdminVerificationStrategyEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

