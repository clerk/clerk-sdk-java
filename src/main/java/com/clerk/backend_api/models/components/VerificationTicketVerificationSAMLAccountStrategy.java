/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.clerk.backend_api.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class VerificationTicketVerificationSAMLAccountStrategy {

    public static final VerificationTicketVerificationSAMLAccountStrategy TICKET = new VerificationTicketVerificationSAMLAccountStrategy("ticket");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, VerificationTicketVerificationSAMLAccountStrategy> values = createValuesMap();
    private static final Map<String, VerificationTicketVerificationSAMLAccountStrategyEnum> enums = createEnumsMap();

    private final String value;

    private VerificationTicketVerificationSAMLAccountStrategy(String value) {
        this.value = value;
    }

    /**
     * Returns a VerificationTicketVerificationSAMLAccountStrategy with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as VerificationTicketVerificationSAMLAccountStrategy
     */ 
    @JsonCreator
    public static VerificationTicketVerificationSAMLAccountStrategy of(String value) {
        synchronized (VerificationTicketVerificationSAMLAccountStrategy.class) {
            return values.computeIfAbsent(value, v -> new VerificationTicketVerificationSAMLAccountStrategy(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<VerificationTicketVerificationSAMLAccountStrategyEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        VerificationTicketVerificationSAMLAccountStrategy other = (VerificationTicketVerificationSAMLAccountStrategy) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "VerificationTicketVerificationSAMLAccountStrategy [value=" + value + "]";
    }

    // return an array just like an enum
    public static VerificationTicketVerificationSAMLAccountStrategy[] values() {
        synchronized (VerificationTicketVerificationSAMLAccountStrategy.class) {
            return values.values().toArray(new VerificationTicketVerificationSAMLAccountStrategy[] {});
        }
    }

    private static final Map<String, VerificationTicketVerificationSAMLAccountStrategy> createValuesMap() {
        Map<String, VerificationTicketVerificationSAMLAccountStrategy> map = new LinkedHashMap<>();
        map.put("ticket", TICKET);
        return map;
    }

    private static final Map<String, VerificationTicketVerificationSAMLAccountStrategyEnum> createEnumsMap() {
        Map<String, VerificationTicketVerificationSAMLAccountStrategyEnum> map = new HashMap<>();
        map.put("ticket", VerificationTicketVerificationSAMLAccountStrategyEnum.TICKET);
        return map;
    }
    
    
    public enum VerificationTicketVerificationSAMLAccountStrategyEnum {

        TICKET("ticket"),;

        private final String value;

        private VerificationTicketVerificationSAMLAccountStrategyEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

