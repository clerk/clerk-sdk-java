/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.clerk.backend_api.models.components;

import com.clerk.backend_api.utils.Utils;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import java.lang.Long;
import java.lang.Override;
import java.lang.String;
import java.util.Optional;


public class AdjustCreditBalanceRequest {
    /**
     * The credit amount in cents. Must be greater than zero.
     */
    @JsonProperty("amount")
    private long amount;

    /**
     * Whether to increase or decrease the credit balance.
     */
    @JsonProperty("action")
    private Action action;

    /**
     * The currency code (e.g. "USD"). Defaults to USD if not provided.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("currency")
    private Optional<String> currency;

    /**
     * A unique key to ensure the adjustment is applied only once. Repeated requests with the same key
     * return the original ledger entry.
     */
    @JsonProperty("idempotency_key")
    private String idempotencyKey;

    /**
     * An optional note to attach to the ledger entry.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("note")
    private Optional<String> note;

    @JsonCreator
    public AdjustCreditBalanceRequest(
            @JsonProperty("amount") long amount,
            @JsonProperty("action") Action action,
            @JsonProperty("currency") Optional<String> currency,
            @JsonProperty("idempotency_key") String idempotencyKey,
            @JsonProperty("note") Optional<String> note) {
        Utils.checkNotNull(amount, "amount");
        Utils.checkNotNull(action, "action");
        Utils.checkNotNull(currency, "currency");
        Utils.checkNotNull(idempotencyKey, "idempotencyKey");
        Utils.checkNotNull(note, "note");
        this.amount = amount;
        this.action = action;
        this.currency = currency;
        this.idempotencyKey = idempotencyKey;
        this.note = note;
    }
    
    public AdjustCreditBalanceRequest(
            long amount,
            Action action,
            String idempotencyKey) {
        this(amount, action, Optional.empty(),
            idempotencyKey, Optional.empty());
    }

    /**
     * The credit amount in cents. Must be greater than zero.
     */
    @JsonIgnore
    public long amount() {
        return amount;
    }

    /**
     * Whether to increase or decrease the credit balance.
     */
    @JsonIgnore
    public Action action() {
        return action;
    }

    /**
     * The currency code (e.g. "USD"). Defaults to USD if not provided.
     */
    @JsonIgnore
    public Optional<String> currency() {
        return currency;
    }

    /**
     * A unique key to ensure the adjustment is applied only once. Repeated requests with the same key
     * return the original ledger entry.
     */
    @JsonIgnore
    public String idempotencyKey() {
        return idempotencyKey;
    }

    /**
     * An optional note to attach to the ledger entry.
     */
    @JsonIgnore
    public Optional<String> note() {
        return note;
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * The credit amount in cents. Must be greater than zero.
     */
    public AdjustCreditBalanceRequest withAmount(long amount) {
        Utils.checkNotNull(amount, "amount");
        this.amount = amount;
        return this;
    }

    /**
     * Whether to increase or decrease the credit balance.
     */
    public AdjustCreditBalanceRequest withAction(Action action) {
        Utils.checkNotNull(action, "action");
        this.action = action;
        return this;
    }

    /**
     * The currency code (e.g. "USD"). Defaults to USD if not provided.
     */
    public AdjustCreditBalanceRequest withCurrency(String currency) {
        Utils.checkNotNull(currency, "currency");
        this.currency = Optional.ofNullable(currency);
        return this;
    }


    /**
     * The currency code (e.g. "USD"). Defaults to USD if not provided.
     */
    public AdjustCreditBalanceRequest withCurrency(Optional<String> currency) {
        Utils.checkNotNull(currency, "currency");
        this.currency = currency;
        return this;
    }

    /**
     * A unique key to ensure the adjustment is applied only once. Repeated requests with the same key
     * return the original ledger entry.
     */
    public AdjustCreditBalanceRequest withIdempotencyKey(String idempotencyKey) {
        Utils.checkNotNull(idempotencyKey, "idempotencyKey");
        this.idempotencyKey = idempotencyKey;
        return this;
    }

    /**
     * An optional note to attach to the ledger entry.
     */
    public AdjustCreditBalanceRequest withNote(String note) {
        Utils.checkNotNull(note, "note");
        this.note = Optional.ofNullable(note);
        return this;
    }


    /**
     * An optional note to attach to the ledger entry.
     */
    public AdjustCreditBalanceRequest withNote(Optional<String> note) {
        Utils.checkNotNull(note, "note");
        this.note = note;
        return this;
    }

    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        AdjustCreditBalanceRequest other = (AdjustCreditBalanceRequest) o;
        return 
            Utils.enhancedDeepEquals(this.amount, other.amount) &&
            Utils.enhancedDeepEquals(this.action, other.action) &&
            Utils.enhancedDeepEquals(this.currency, other.currency) &&
            Utils.enhancedDeepEquals(this.idempotencyKey, other.idempotencyKey) &&
            Utils.enhancedDeepEquals(this.note, other.note);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            amount, action, currency,
            idempotencyKey, note);
    }
    
    @Override
    public String toString() {
        return Utils.toString(AdjustCreditBalanceRequest.class,
                "amount", amount,
                "action", action,
                "currency", currency,
                "idempotencyKey", idempotencyKey,
                "note", note);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private Long amount;

        private Action action;

        private Optional<String> currency = Optional.empty();

        private String idempotencyKey;

        private Optional<String> note = Optional.empty();

        private Builder() {
          // force use of static builder() method
        }


        /**
         * The credit amount in cents. Must be greater than zero.
         */
        public Builder amount(long amount) {
            Utils.checkNotNull(amount, "amount");
            this.amount = amount;
            return this;
        }


        /**
         * Whether to increase or decrease the credit balance.
         */
        public Builder action(Action action) {
            Utils.checkNotNull(action, "action");
            this.action = action;
            return this;
        }


        /**
         * The currency code (e.g. "USD"). Defaults to USD if not provided.
         */
        public Builder currency(String currency) {
            Utils.checkNotNull(currency, "currency");
            this.currency = Optional.ofNullable(currency);
            return this;
        }

        /**
         * The currency code (e.g. "USD"). Defaults to USD if not provided.
         */
        public Builder currency(Optional<String> currency) {
            Utils.checkNotNull(currency, "currency");
            this.currency = currency;
            return this;
        }


        /**
         * A unique key to ensure the adjustment is applied only once. Repeated requests with the same key
         * return the original ledger entry.
         */
        public Builder idempotencyKey(String idempotencyKey) {
            Utils.checkNotNull(idempotencyKey, "idempotencyKey");
            this.idempotencyKey = idempotencyKey;
            return this;
        }


        /**
         * An optional note to attach to the ledger entry.
         */
        public Builder note(String note) {
            Utils.checkNotNull(note, "note");
            this.note = Optional.ofNullable(note);
            return this;
        }

        /**
         * An optional note to attach to the ledger entry.
         */
        public Builder note(Optional<String> note) {
            Utils.checkNotNull(note, "note");
            this.note = note;
            return this;
        }

        public AdjustCreditBalanceRequest build() {

            return new AdjustCreditBalanceRequest(
                amount, action, currency,
                idempotencyKey, note);
        }

    }
}
