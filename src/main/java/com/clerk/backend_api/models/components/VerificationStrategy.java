/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.clerk.backend_api.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class VerificationStrategy {

    public static final VerificationStrategy ADMIN = new VerificationStrategy("admin");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, VerificationStrategy> values = createValuesMap();
    private static final Map<String, VerificationStrategyEnum> enums = createEnumsMap();

    private final String value;

    private VerificationStrategy(String value) {
        this.value = value;
    }

    /**
     * Returns a VerificationStrategy with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as VerificationStrategy
     */ 
    @JsonCreator
    public static VerificationStrategy of(String value) {
        synchronized (VerificationStrategy.class) {
            return values.computeIfAbsent(value, v -> new VerificationStrategy(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<VerificationStrategyEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        VerificationStrategy other = (VerificationStrategy) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "VerificationStrategy [value=" + value + "]";
    }

    // return an array just like an enum
    public static VerificationStrategy[] values() {
        synchronized (VerificationStrategy.class) {
            return values.values().toArray(new VerificationStrategy[] {});
        }
    }

    private static final Map<String, VerificationStrategy> createValuesMap() {
        Map<String, VerificationStrategy> map = new LinkedHashMap<>();
        map.put("admin", ADMIN);
        return map;
    }

    private static final Map<String, VerificationStrategyEnum> createEnumsMap() {
        Map<String, VerificationStrategyEnum> map = new HashMap<>();
        map.put("admin", VerificationStrategyEnum.ADMIN);
        return map;
    }
    
    
    public enum VerificationStrategyEnum {

        ADMIN("admin"),;

        private final String value;

        private VerificationStrategyEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

