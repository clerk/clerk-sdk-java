/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.clerk.backend_api.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.Override;
import java.lang.String;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * Wrapper for an "open" enum that can handle unknown values from API responses
 * without runtime errors. Instances are immutable singletons with reference equality.
 * Use {@code asEnum()} for switch expressions.
 */
public class VerificationTicketVerificationStrategy {

    public static final VerificationTicketVerificationStrategy TICKET = new VerificationTicketVerificationStrategy("ticket");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, VerificationTicketVerificationStrategy> values = createValuesMap();
    private static final Map<String, VerificationTicketVerificationStrategyEnum> enums = createEnumsMap();

    private final String value;

    private VerificationTicketVerificationStrategy(String value) {
        this.value = value;
    }

    /**
     * Returns a VerificationTicketVerificationStrategy with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as VerificationTicketVerificationStrategy
     */ 
    @JsonCreator
    public static VerificationTicketVerificationStrategy of(String value) {
        synchronized (VerificationTicketVerificationStrategy.class) {
            return values.computeIfAbsent(value, v -> new VerificationTicketVerificationStrategy(v));
        }
    }

    @JsonValue
    public String value() {
        return value;
    }

    public Optional<VerificationTicketVerificationStrategyEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        VerificationTicketVerificationStrategy other = (VerificationTicketVerificationStrategy) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "VerificationTicketVerificationStrategy [value=" + value + "]";
    }

    // return an array just like an enum
    public static VerificationTicketVerificationStrategy[] values() {
        synchronized (VerificationTicketVerificationStrategy.class) {
            return values.values().toArray(new VerificationTicketVerificationStrategy[] {});
        }
    }

    private static final Map<String, VerificationTicketVerificationStrategy> createValuesMap() {
        Map<String, VerificationTicketVerificationStrategy> map = new LinkedHashMap<>();
        map.put("ticket", TICKET);
        return map;
    }

    private static final Map<String, VerificationTicketVerificationStrategyEnum> createEnumsMap() {
        Map<String, VerificationTicketVerificationStrategyEnum> map = new HashMap<>();
        map.put("ticket", VerificationTicketVerificationStrategyEnum.TICKET);
        return map;
    }
    
    
    public enum VerificationTicketVerificationStrategyEnum {

        TICKET("ticket"),;

        private final String value;

        private VerificationTicketVerificationStrategyEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}

