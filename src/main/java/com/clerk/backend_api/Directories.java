/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.clerk.backend_api;

import static com.clerk.backend_api.operations.Operations.RequestOperation;

import com.clerk.backend_api.models.operations.CreateDirectoryGroupRoleMappingRequest;
import com.clerk.backend_api.models.operations.CreateDirectoryGroupRoleMappingRequestBody;
import com.clerk.backend_api.models.operations.CreateDirectoryGroupRoleMappingRequestBuilder;
import com.clerk.backend_api.models.operations.CreateDirectoryGroupRoleMappingResponse;
import com.clerk.backend_api.models.operations.CreateDirectoryRequestBody;
import com.clerk.backend_api.models.operations.CreateDirectoryRequestBuilder;
import com.clerk.backend_api.models.operations.CreateDirectoryResponse;
import com.clerk.backend_api.models.operations.DeleteDirectoryGroupRoleMappingRequest;
import com.clerk.backend_api.models.operations.DeleteDirectoryGroupRoleMappingRequestBuilder;
import com.clerk.backend_api.models.operations.DeleteDirectoryGroupRoleMappingResponse;
import com.clerk.backend_api.models.operations.DeleteDirectoryRequest;
import com.clerk.backend_api.models.operations.DeleteDirectoryRequestBuilder;
import com.clerk.backend_api.models.operations.DeleteDirectoryResponse;
import com.clerk.backend_api.models.operations.GetDirectoryRequest;
import com.clerk.backend_api.models.operations.GetDirectoryRequestBuilder;
import com.clerk.backend_api.models.operations.GetDirectoryResponse;
import com.clerk.backend_api.models.operations.ListDirectoriesRequest;
import com.clerk.backend_api.models.operations.ListDirectoriesRequestBuilder;
import com.clerk.backend_api.models.operations.ListDirectoriesResponse;
import com.clerk.backend_api.models.operations.ListDirectoryGroupRoleMappingsRequest;
import com.clerk.backend_api.models.operations.ListDirectoryGroupRoleMappingsRequestBuilder;
import com.clerk.backend_api.models.operations.ListDirectoryGroupRoleMappingsResponse;
import com.clerk.backend_api.models.operations.ReplaceDirectoryGroupRoleMappingsRequest;
import com.clerk.backend_api.models.operations.ReplaceDirectoryGroupRoleMappingsRequestBody;
import com.clerk.backend_api.models.operations.ReplaceDirectoryGroupRoleMappingsRequestBuilder;
import com.clerk.backend_api.models.operations.ReplaceDirectoryGroupRoleMappingsResponse;
import com.clerk.backend_api.models.operations.RotateDirectoryAPIKeyRequest;
import com.clerk.backend_api.models.operations.RotateDirectoryAPIKeyRequestBuilder;
import com.clerk.backend_api.models.operations.RotateDirectoryAPIKeyResponse;
import com.clerk.backend_api.models.operations.UpdateDirectoryRequest;
import com.clerk.backend_api.models.operations.UpdateDirectoryRequestBody;
import com.clerk.backend_api.models.operations.UpdateDirectoryRequestBuilder;
import com.clerk.backend_api.models.operations.UpdateDirectoryResponse;
import com.clerk.backend_api.operations.CreateDirectory;
import com.clerk.backend_api.operations.CreateDirectoryGroupRoleMapping;
import com.clerk.backend_api.operations.DeleteDirectory;
import com.clerk.backend_api.operations.DeleteDirectoryGroupRoleMapping;
import com.clerk.backend_api.operations.GetDirectory;
import com.clerk.backend_api.operations.ListDirectories;
import com.clerk.backend_api.operations.ListDirectoryGroupRoleMappings;
import com.clerk.backend_api.operations.ReplaceDirectoryGroupRoleMappings;
import com.clerk.backend_api.operations.RotateDirectoryAPIKey;
import com.clerk.backend_api.operations.UpdateDirectory;
import com.clerk.backend_api.utils.Headers;
import com.clerk.backend_api.utils.Options;
import java.lang.Long;
import java.lang.String;
import java.util.Optional;


public class Directories {
    private static final Headers _headers = Headers.EMPTY;
    private final SDKConfiguration sdkConfiguration;

    Directories(SDKConfiguration sdkConfiguration) {
        this.sdkConfiguration = sdkConfiguration;
    }

    /**
     * List all directories
     * 
     * <p>Returns a list of all directories for the instance.
     * 
     * @return The call builder
     */
    public ListDirectoriesRequestBuilder list() {
        return new ListDirectoriesRequestBuilder(sdkConfiguration);
    }

    /**
     * List all directories
     * 
     * <p>Returns a list of all directories for the instance.
     * 
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ListDirectoriesResponse listDirect() {
        return list(Optional.empty(), Optional.empty(), Optional.empty());
    }

    /**
     * List all directories
     * 
     * <p>Returns a list of all directories for the instance.
     * 
     * @param limit Applies a limit to the number of results returned.
     *         Can be used for paginating the results together with `offset`.
     * @param offset Skip the first `offset` results when paginating.
     *         Needs to be an integer greater or equal to zero.
     *         To be used in conjunction with `limit`.
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ListDirectoriesResponse list(
            Optional<Long> limit, Optional<Long> offset,
            Optional<Options> options) {
        ListDirectoriesRequest request =
            ListDirectoriesRequest
                .builder()
                .limit(limit)
                .offset(offset)
                .build();
        RequestOperation<ListDirectoriesRequest, ListDirectoriesResponse> operation
              = new ListDirectories.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Create a directory
     * 
     * <p>Create a new directory for the instance.
     * 
     * @return The call builder
     */
    public CreateDirectoryRequestBuilder create() {
        return new CreateDirectoryRequestBuilder(sdkConfiguration);
    }

    /**
     * Create a directory
     * 
     * <p>Create a new directory for the instance.
     * 
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public CreateDirectoryResponse createDirect() {
        return create(Optional.empty(), Optional.empty());
    }

    /**
     * Create a directory
     * 
     * <p>Create a new directory for the instance.
     * 
     * @param request The request object containing all the parameters for the API call.
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public CreateDirectoryResponse create(Optional<? extends CreateDirectoryRequestBody> request, Optional<Options> options) {
        RequestOperation<Optional<? extends CreateDirectoryRequestBody>, CreateDirectoryResponse> operation
              = new CreateDirectory.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Retrieve a directory
     * 
     * <p>Returns the details of a directory.
     * 
     * @return The call builder
     */
    public GetDirectoryRequestBuilder get() {
        return new GetDirectoryRequestBuilder(sdkConfiguration);
    }

    /**
     * Retrieve a directory
     * 
     * <p>Returns the details of a directory.
     * 
     * @param directoryId The ID of the directory to retrieve
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public GetDirectoryResponse get(String directoryId) {
        return get(directoryId, Optional.empty());
    }

    /**
     * Retrieve a directory
     * 
     * <p>Returns the details of a directory.
     * 
     * @param directoryId The ID of the directory to retrieve
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public GetDirectoryResponse get(String directoryId, Optional<Options> options) {
        GetDirectoryRequest request =
            GetDirectoryRequest
                .builder()
                .directoryId(directoryId)
                .build();
        RequestOperation<GetDirectoryRequest, GetDirectoryResponse> operation
              = new GetDirectory.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Update a directory
     * 
     * <p>Updates a directory.
     * 
     * @return The call builder
     */
    public UpdateDirectoryRequestBuilder update() {
        return new UpdateDirectoryRequestBuilder(sdkConfiguration);
    }

    /**
     * Update a directory
     * 
     * <p>Updates a directory.
     * 
     * @param directoryId The ID of the directory to update
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public UpdateDirectoryResponse update(String directoryId) {
        return update(directoryId, Optional.empty(), Optional.empty());
    }

    /**
     * Update a directory
     * 
     * <p>Updates a directory.
     * 
     * @param directoryId The ID of the directory to update
     * @param requestBody 
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public UpdateDirectoryResponse update(
            String directoryId, Optional<? extends UpdateDirectoryRequestBody> requestBody,
            Optional<Options> options) {
        UpdateDirectoryRequest request =
            UpdateDirectoryRequest
                .builder()
                .directoryId(directoryId)
                .requestBody(requestBody)
                .build();
        RequestOperation<UpdateDirectoryRequest, UpdateDirectoryResponse> operation
              = new UpdateDirectory.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Delete a directory
     * 
     * <p>Deletes a directory and stops provisioning for it. Provisioning requests authenticated
     * with the directory's API key are rejected afterwards.
     * 
     * @return The call builder
     */
    public DeleteDirectoryRequestBuilder delete() {
        return new DeleteDirectoryRequestBuilder(sdkConfiguration);
    }

    /**
     * Delete a directory
     * 
     * <p>Deletes a directory and stops provisioning for it. Provisioning requests authenticated
     * with the directory's API key are rejected afterwards.
     * 
     * @param directoryId The ID of the directory to delete
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public DeleteDirectoryResponse delete(String directoryId) {
        return delete(directoryId, Optional.empty());
    }

    /**
     * Delete a directory
     * 
     * <p>Deletes a directory and stops provisioning for it. Provisioning requests authenticated
     * with the directory's API key are rejected afterwards.
     * 
     * @param directoryId The ID of the directory to delete
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public DeleteDirectoryResponse delete(String directoryId, Optional<Options> options) {
        DeleteDirectoryRequest request =
            DeleteDirectoryRequest
                .builder()
                .directoryId(directoryId)
                .build();
        RequestOperation<DeleteDirectoryRequest, DeleteDirectoryResponse> operation
              = new DeleteDirectory.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Rotate a directory's API key
     * 
     * <p>Generates a new API key for the directory and returns it in the `api_key` field.
     * This is the only way to obtain the key after creation, so make sure to update it in
     * your identity provider. The previous key remains valid for a short grace period before
     * it expires.
     * 
     * @return The call builder
     */
    public RotateDirectoryAPIKeyRequestBuilder rotateApiKey() {
        return new RotateDirectoryAPIKeyRequestBuilder(sdkConfiguration);
    }

    /**
     * Rotate a directory's API key
     * 
     * <p>Generates a new API key for the directory and returns it in the `api_key` field.
     * This is the only way to obtain the key after creation, so make sure to update it in
     * your identity provider. The previous key remains valid for a short grace period before
     * it expires.
     * 
     * @param directoryId The ID of the directory whose API key to rotate
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public RotateDirectoryAPIKeyResponse rotateApiKey(String directoryId) {
        return rotateApiKey(directoryId, Optional.empty());
    }

    /**
     * Rotate a directory's API key
     * 
     * <p>Generates a new API key for the directory and returns it in the `api_key` field.
     * This is the only way to obtain the key after creation, so make sure to update it in
     * your identity provider. The previous key remains valid for a short grace period before
     * it expires.
     * 
     * @param directoryId The ID of the directory whose API key to rotate
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public RotateDirectoryAPIKeyResponse rotateApiKey(String directoryId, Optional<Options> options) {
        RotateDirectoryAPIKeyRequest request =
            RotateDirectoryAPIKeyRequest
                .builder()
                .directoryId(directoryId)
                .build();
        RequestOperation<RotateDirectoryAPIKeyRequest, RotateDirectoryAPIKeyResponse> operation
              = new RotateDirectoryAPIKey.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * List directory group role mappings
     * 
     * <p>Returns the list of directory group to organization role mappings for a directory, ordered by
     * precedence.
     * 
     * @return The call builder
     */
    public ListDirectoryGroupRoleMappingsRequestBuilder listGroupRoleMappings() {
        return new ListDirectoryGroupRoleMappingsRequestBuilder(sdkConfiguration);
    }

    /**
     * List directory group role mappings
     * 
     * <p>Returns the list of directory group to organization role mappings for a directory, ordered by
     * precedence.
     * 
     * @param directoryId The ID of the directory.
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ListDirectoryGroupRoleMappingsResponse listGroupRoleMappings(String directoryId) {
        return listGroupRoleMappings(directoryId, Optional.empty());
    }

    /**
     * List directory group role mappings
     * 
     * <p>Returns the list of directory group to organization role mappings for a directory, ordered by
     * precedence.
     * 
     * @param directoryId The ID of the directory.
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ListDirectoryGroupRoleMappingsResponse listGroupRoleMappings(String directoryId, Optional<Options> options) {
        ListDirectoryGroupRoleMappingsRequest request =
            ListDirectoryGroupRoleMappingsRequest
                .builder()
                .directoryId(directoryId)
                .build();
        RequestOperation<ListDirectoryGroupRoleMappingsRequest, ListDirectoryGroupRoleMappingsResponse> operation
              = new ListDirectoryGroupRoleMappings.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Create a directory group role mapping
     * 
     * <p>Creates a new directory group to organization role mapping for a directory.
     * Group role mapping must be enabled on the directory.
     * 
     * @return The call builder
     */
    public CreateDirectoryGroupRoleMappingRequestBuilder createGroupRoleMapping() {
        return new CreateDirectoryGroupRoleMappingRequestBuilder(sdkConfiguration);
    }

    /**
     * Create a directory group role mapping
     * 
     * <p>Creates a new directory group to organization role mapping for a directory.
     * Group role mapping must be enabled on the directory.
     * 
     * @param directoryId The ID of the directory.
     * @param requestBody 
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public CreateDirectoryGroupRoleMappingResponse createGroupRoleMapping(String directoryId, CreateDirectoryGroupRoleMappingRequestBody requestBody) {
        return createGroupRoleMapping(directoryId, requestBody, Optional.empty());
    }

    /**
     * Create a directory group role mapping
     * 
     * <p>Creates a new directory group to organization role mapping for a directory.
     * Group role mapping must be enabled on the directory.
     * 
     * @param directoryId The ID of the directory.
     * @param requestBody 
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public CreateDirectoryGroupRoleMappingResponse createGroupRoleMapping(
            String directoryId, CreateDirectoryGroupRoleMappingRequestBody requestBody,
            Optional<Options> options) {
        CreateDirectoryGroupRoleMappingRequest request =
            CreateDirectoryGroupRoleMappingRequest
                .builder()
                .directoryId(directoryId)
                .requestBody(requestBody)
                .build();
        RequestOperation<CreateDirectoryGroupRoleMappingRequest, CreateDirectoryGroupRoleMappingResponse> operation
              = new CreateDirectoryGroupRoleMapping.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Replace directory group role mappings
     * 
     * <p>Replaces the entire set of directory group role mappings for a directory. The position of
     * each item in the `mappings` array determines its precedence (the first item gets
     * precedence 1). Passing an empty array removes all mappings. Group role mapping must be
     * enabled on the directory.
     * 
     * @return The call builder
     */
    public ReplaceDirectoryGroupRoleMappingsRequestBuilder replaceGroupRoleMappings() {
        return new ReplaceDirectoryGroupRoleMappingsRequestBuilder(sdkConfiguration);
    }

    /**
     * Replace directory group role mappings
     * 
     * <p>Replaces the entire set of directory group role mappings for a directory. The position of
     * each item in the `mappings` array determines its precedence (the first item gets
     * precedence 1). Passing an empty array removes all mappings. Group role mapping must be
     * enabled on the directory.
     * 
     * @param directoryId The ID of the directory.
     * @param requestBody 
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ReplaceDirectoryGroupRoleMappingsResponse replaceGroupRoleMappings(String directoryId, ReplaceDirectoryGroupRoleMappingsRequestBody requestBody) {
        return replaceGroupRoleMappings(directoryId, requestBody, Optional.empty());
    }

    /**
     * Replace directory group role mappings
     * 
     * <p>Replaces the entire set of directory group role mappings for a directory. The position of
     * each item in the `mappings` array determines its precedence (the first item gets
     * precedence 1). Passing an empty array removes all mappings. Group role mapping must be
     * enabled on the directory.
     * 
     * @param directoryId The ID of the directory.
     * @param requestBody 
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public ReplaceDirectoryGroupRoleMappingsResponse replaceGroupRoleMappings(
            String directoryId, ReplaceDirectoryGroupRoleMappingsRequestBody requestBody,
            Optional<Options> options) {
        ReplaceDirectoryGroupRoleMappingsRequest request =
            ReplaceDirectoryGroupRoleMappingsRequest
                .builder()
                .directoryId(directoryId)
                .requestBody(requestBody)
                .build();
        RequestOperation<ReplaceDirectoryGroupRoleMappingsRequest, ReplaceDirectoryGroupRoleMappingsResponse> operation
              = new ReplaceDirectoryGroupRoleMappings.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

    /**
     * Delete a directory group role mapping
     * 
     * <p>Deletes a single directory group role mapping. Group role mapping must be enabled on the
     * directory.
     * 
     * @return The call builder
     */
    public DeleteDirectoryGroupRoleMappingRequestBuilder deleteGroupRoleMapping() {
        return new DeleteDirectoryGroupRoleMappingRequestBuilder(sdkConfiguration);
    }

    /**
     * Delete a directory group role mapping
     * 
     * <p>Deletes a single directory group role mapping. Group role mapping must be enabled on the
     * directory.
     * 
     * @param directoryId The ID of the directory.
     * @param mappingId The ID of the directory group role mapping to delete.
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public DeleteDirectoryGroupRoleMappingResponse deleteGroupRoleMapping(String directoryId, String mappingId) {
        return deleteGroupRoleMapping(directoryId, mappingId, Optional.empty());
    }

    /**
     * Delete a directory group role mapping
     * 
     * <p>Deletes a single directory group role mapping. Group role mapping must be enabled on the
     * directory.
     * 
     * @param directoryId The ID of the directory.
     * @param mappingId The ID of the directory group role mapping to delete.
     * @param options additional options
     * @return The response from the API call
     * @throws RuntimeException subclass if the API call fails
     */
    public DeleteDirectoryGroupRoleMappingResponse deleteGroupRoleMapping(
            String directoryId, String mappingId,
            Optional<Options> options) {
        DeleteDirectoryGroupRoleMappingRequest request =
            DeleteDirectoryGroupRoleMappingRequest
                .builder()
                .directoryId(directoryId)
                .mappingId(mappingId)
                .build();
        RequestOperation<DeleteDirectoryGroupRoleMappingRequest, DeleteDirectoryGroupRoleMappingResponse> operation
              = new DeleteDirectoryGroupRoleMapping.Sync(sdkConfiguration, options, _headers);
        return operation.handleResponse(operation.doRequest(request));
    }

}
